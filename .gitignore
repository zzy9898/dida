node_modules/
dist/
unpackage/
.env
*.log
.DS_Store

# Claude Code 本地工具配置（不入库）
.claude/
.mcp.json

# >>> @tencent-rtc/chat-custom-integration（自动添加，可手动调整）
# 本地配置 + 会话状态（由 dispatcher 在运行时写入）。
# 注意：knowledge-base 已迁移到用户级 ~/.trtc-chat/，不再出现在项目里。
/.trtc-chat/
# <<< @tencent-rtc/chat-custom-integration
